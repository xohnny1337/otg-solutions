---
import { cn } from '@/lib/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={cn('mx-auto w-full max-w-6xl px-6', className)}>
  <slot />
</div>
